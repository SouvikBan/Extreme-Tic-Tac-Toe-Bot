import sys

class TEAM46:
    def BestMove(self, old_move, maxDepth):

		validCells = board.find_valid_move_cells(old_move)

        if depth == maxDepth:
			# return utility value

		maxMove = (flag==self.who)

        if maxMove:
        
        
        